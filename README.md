# Go 1.22 workspaces integration test

This test case is a migration of
`github.com/cachito-testing/gomod-pandemonium`@`go-1.22-workspaces`

This test case exercises a Go workspace-style module that pulls in different dependencies depending on the build environment.


